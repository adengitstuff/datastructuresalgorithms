package nameIndexer;

import java.io.File;

public class nameIndexer {

    // 1. Ask TA's if this package setup and project setup is right
    
    public static void main(String[] args) {
    
        // first check args are legit and not null. does tab count
        // as 1 arg or what?
        
        // index    gisrecordfile.txt
        
        if (args[0].contentEquals("index")) {
            
            // checks if legit:
                // args no more than args[2]. check if args2 exists?
            
            
            // open the gisrecordfile
                // check if filewriter, reader, or raf
                // wrap in a tryblock. catch.
                // decide if args[1] or [args[2]
                
            
           // PARSER:
            
        }
        
        if (args[0].contentEquals("what_is")){
            
        }
        
        if (args[0].contentEquals("debug")){
            
        }
        
        if (args[0].contentEquals("quit")){
            // still make sure there's a tab after
        }

    }
    
    private void nameParsie(String fi) {
        
        // Open up either a raf, filewriter. Put in a try block
        
        
                // Parsing part:
        
        
       // If the line begins with a ; character, move to the next line?
        
       // If the line starts with '\n', just move to the next line?
        
       
        
        
            //NAME END LOGIC:
        
       //           real line: a GIS Parser line
        //          Get the current offset, get the recordName
       //           put 'em in a hashie tablie
       //           collisions?
        
    }
    
    

}
