


	Hash table: hashTable.java
		   Stores nameEntry objects

	Quadtable:  prQuadTree.java

		    Stores coordinate objects

	Buffer pool:
		   bufferPool.java

	GIS Record:
		   Used raw strings instead of creating a GIS record class. Thoguth it was a lot 
		 of fields to store at once for each object

	Feature name index:
		  nameIndexer.java

	Location index:
		  gigaIndex.java (both indexes)